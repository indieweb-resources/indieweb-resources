{%- from "component.njk" import component with context -%}

<!doctype html>

<html lang="{{ page.lang }}" dir="{{ dir }}" class="antialiased">
  <head>
    <meta charset="utf-8">
    <title>{{ title }}</title>
    <link rel="stylesheet" type="text/css" href="/styles/style.css">
  </head>

  <body>

    {{ content | safe }}

  </body>
</html>